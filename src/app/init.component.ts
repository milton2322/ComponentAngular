import { Component } from '@angular/core';

@Component({
  selector: 'init-component',
  //esto va a contener todos los componentes que vamos ruteando por la URL
  template: '<router-outlet></router-outlet>'
})

export class InitComponent {}
